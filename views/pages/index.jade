extends ../layout

block content
  .container
    .row
      each item in movies
        .col-md-2
          .thumbnail
            a(href='/movie/#{item._id}')
              img(src='#{item.img}')
          .caption
            h3 #{item.title}
            p: a.btn.btn-primary(href='/',role='button') 呵呵呵

