extends ../layout

block content
  .container
    .row
      each item in movies
        .col-md-2
          h2=item.name
          //.thumbnail
            a(href='/movie/#{item.name}')
              img(src='#{item.name}')
          .caption
            h3 age:#{item.age}
            p: a.btn.btn-primary(href='/',role='button') 呵呵呵

